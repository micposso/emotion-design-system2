Level: Molecule

Purpose: Combine atoms into small, reusable groups (form-field, search-bar, card-with-actions).

Rules:
- Keep composition simple and explicit about atom dependencies.
- State the atomic level before adding new components.
