Level: Organism

Purpose: Larger sections composed of molecules and atoms (header, footer, navigation, content-grid).

Rules:
- Organisms may depend on molecules and atoms; document dependencies in the component folder.
- Use semantic container elements like `header`, `nav`, `main`, `section`.
