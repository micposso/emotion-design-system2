Level: Atom

Purpose: A single-purpose, reusable `DownloadButton` used to trigger file downloads. Atoms are small, self-contained UI controls with no dependencies on other components.

Rules:
- Use a semantic `<button>` element.
- Apply BEM class names: `download-button`, `download-button__icon`, `download-button--primary`.
- Use design tokens from `src/styles/tokens.css` for colors and focus styles.
